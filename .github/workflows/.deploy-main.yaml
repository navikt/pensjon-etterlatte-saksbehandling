name: .deploy-main.yaml

on:
  workflow_call:
    inputs:
      applikasjon:
        description: 'Navnet på applikasjonen'
        required: true
        type: string

env:
  image: europe-north1-docker.pkg.dev/nais-management-233d/etterlatte/${{ github.repository }}-etterlatte-${{ inputs.applikasjon }}:2023.05.28-14.16-cd56ef7

jobs:
  deploy-main:
    name: deploy-main
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          CLUSTER: dev-gcp
          RESOURCE: apps/etterlatte-${{ inputs.applikasjon }}/.nais/dev.yaml
          VAR: image=${{ env.image }}
