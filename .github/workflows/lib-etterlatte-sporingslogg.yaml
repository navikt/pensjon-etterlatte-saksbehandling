name: etterlatte-sporingslogg

on:
  push:
    branches:
      - main
    paths:
      - libs/etterlatte-sporingslogg/**
  pull_request:
    branches:
      - main
    paths:
      - libs/etterlatte-sporingslogg/**

jobs:
  test:
    if: github.event_name == 'pull_request'
    uses: ./.github/workflows/.lib-test.yaml
    secrets: inherit
