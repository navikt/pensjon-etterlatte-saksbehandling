name: ktor2client-auth-clientcredentials

on:
  push:
    branches:
      - main
    paths:
      - libs/ktor2client-auth-clientcredentials/**
  pull_request:
    branches:
      - main
    paths:
      - libs/ktor2client-auth-clientcredentials/**

jobs:
  test:
    if: github.event_name == 'pull_request'
    uses: ./.github/workflows/.lib-test.yaml
    secrets: inherit
