name: testdata

on:
  push:
    branches:
      - main
    paths:
      - libs/testdata/**
  pull_request:
    branches:
      - main
    paths:
      - libs/testdata/**

jobs:
  test:
    if: github.event_name == 'pull_request'
    uses: ./.github/workflows/.lib-test.yaml
    secrets: inherit
