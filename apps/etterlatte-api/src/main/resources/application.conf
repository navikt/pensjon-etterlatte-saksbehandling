
no.nav.security.jwt {
  issuers = [
      {
         issuer_name = azure
         discoveryurl = ${?AZURE_APP_WELL_KNOWN_URL}
         accepted_audience = ${?AZURE_APP_CLIENT_ID}
      }
  ]
}

no.nav.etterlatte.tjenester.behandling.url = ${ETTERLATTE_BEHANDLING_URL}