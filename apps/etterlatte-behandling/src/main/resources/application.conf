no.nav.security.jwt {
  issuers = [
    {
      issuer_name = azure
      discoveryurl = ${?AZURE_APP_WELL_KNOWN_URL}
      accepted_audience = ${?AZURE_APP_CLIENT_ID}
    }
  ]
}

azure.app.client.id = ${?AZURE_APP_CLIENT_ID}
azure.app.client.secret = ${?AZURE_APP_CLIENT_SECRET}
azure.app.jwk = ${?AZURE_APP_JWK}
azure.app.well.known.url = ${?AZURE_APP_WELL_KNOWN_URL}

vedtak.client.id = ${?ETTERLATTE_VEDTAK_CLIENT_ID}
vedtak.resource.url = ${?ETTERLATTE_VEDTAK_URL}

beregning.client.id = ${?ETTERLATTE_BEREGNING_CLIENT_ID}
beregning.resource.url = ${?ETTERLATTE_BEREGNING_URL}

trygdetid.client.id = ${?ETTERLATTE_TRYGDETID_CLIENT_ID}
trygdetid.resource.url = ${?ETTERLATTE_TRYGDETID_URL}

tilbakekreving.resource.url = ${?ETTERLATTE_TILBAKEKREVING_URL}
tilbakekreving.azure.scope = ${?AZURE_SCOPE_ETTERLATTE_TILBAKEKREVING}

inntektskomponenten.url = ${?INNTEKTSKOMPONENT_URL}
inntektskomponenten.scope = ${?INNTEKTSKOMPONENT_OUTBOUND_SCOPE}

etteroppgjoer.svarfrist = ${?ETTEROPPGJOER_SVARFRIST}

sigrun.url = ${?SIGRUN_URL}
sigrun.scope = ${?SIGRUN_OUTBOUND_SCOPE}

oppgave.client.id = ${?OPPGAVE_CLIENT_ID}
oppgave.resource.url = ${?OPPGAVE_URL}

pdl.client.id = ${?ETTERLATTE_PDL_CLIENT_ID}
pdl.azure.scope = ${?PDLTJENESTER_AZURE_SCOPE}
pdltjenester.url = ${?ETTERLATTE_PDLTJENESTER_URL}

funksjonsbrytere.unleash.applicationName = ${?NAIS_APP_NAME}
funksjonsbrytere.unleash.host = ${?UNLEASH_SERVER_API_URL}
funksjonsbrytere.unleash.token = ${?UNLEASH_SERVER_API_TOKEN}

navansatt.azure.scope = ${?ETTERLATTE_NAVANSATT_OUTBOUND_SCOPE}

skjerming.url = ${?SKJERMING_URL}
skjerming.azure.scope = ${?AZURE_SCOPE_SKJERMEDE_PIP}

brev-api.resource.url = ${?ETTERLATTE_BREV_API_URL}
brev-api.client.id = ${?ETTERLATTE_BREV_API_CLIENT_ID}

klage.azure.scope = ${?AZURE_SCOPE_ETTERLATTE_KLAGE}
klage.resource.url = ${?ETTERLATTE_KLAGE_API_URL}

pen.client.id = ${?PEN_CLIENT_ID}
pen.client.url = ${?PEN_URL}

krr.url = ${?KRR_ENDPOINT_URL}
krr.scope = ${?KRR_SCOPE}

entraProxy.url = ${?ENTRA_PROXY_URL}
entraProxy.scope = ${?ENTRA_PROXY_SCOPE}

kodeverk.client.id = ${?KODEVERK_CLIENT_ID}
kodeverk.resource.url = ${?KODEVERK_URL}
applicationName = ${?NAIS_APP_NAME}

arbeidOgInntekt.url = ${?ARBEID_OG_INNTEKT_URL}