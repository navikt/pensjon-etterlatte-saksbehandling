no.nav.security.jwt {
  issuers = [
      {
         issuer_name = azure
         discoveryurl = ${?AZURE_APP_WELL_KNOWN_URL}
         accepted_audience = ${?AZURE_APP_CLIENT_ID}
      }
  ]
}

serviceuser {
    name = "srvbarnepensjon"
    password = "srv-password"
    name = ${?SERVICEUSER_USERNAME}
    password = ${?SERVICEUSER_PASSWORD}
}

sts {
    url = "http://localhost:8089/rest/v1/sts/token"
    url = ${?STS_URL}
}

azure.app.client.id = ${AZURE_APP_CLIENT_ID}
azure.app.client.secret = ${AZURE_APP_CLIENT_SECRET}
azure.app.well.known.url = ${AZURE_APP_WELL_KNOWN_URL}
