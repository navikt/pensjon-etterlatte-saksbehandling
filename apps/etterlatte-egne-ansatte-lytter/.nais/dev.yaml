apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: etterlatte-egne-ansatte-lytter
  namespace: etterlatte
  labels:
    team: etterlatte
spec:
  image: {{image}}
  port: 8080
  liveness:
    initialDelay: 5
    path: /isalive
  readiness:
    initialDelay: 5
    path: /isready
  strategy:
    type: Recreate
  kafka:
    pool: nav-dev
  azure:
    application:
      enabled: true
  replicas:
    cpuThresholdPercentage: 90
    max: 1
    min: 1
  env:
    - name: KAFKA_RAPID_TOPIC
      value: etterlatte.dodsmelding
    - name: KAFKA_RESET_POLICY
      value: earliest
    - name: KAFKA_CONSUMER_GROUP_ID
      value: dodsmeldingfraleesah
    - name: LEESAH_KAFKA_GROUP_ID
      value: "etterlatte-v1"
    - name: LEESAH_TOPIC_PERSON
      value: "pdl.leesah-v1"
  accessPolicy:
    outbound:
      rules:
        - application: etterlatte-pdltjenester #TODO rm