apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: etterlatte-institusjonsopphold
  namespace: etterlatte
  labels:
    team: etterlatte
spec:
  image: {{image}}
  port: 8080
  liveness:
    initialDelay: 5
    path: /health/isalive
  readiness:
    initialDelay: 5
    path: /health/isready
  strategy:
    type: Recreate
  kafka:
    pool: nav-prod
  azure:
    application:
      enabled: true
  replicas:
    cpuThresholdPercentage: 90
    max: 1
    min: 1
  env:
    - name: KAFKA_RESET_POLICY
      value: earliest
    - name: INSTITUSJON_OPPHOLD_GROUP_ID
      value: "etterlatte-v1"
    - name: SKJERMING_TOPIC
      value: institusjon-opphold-hendelser
    - name: BEHANDLING_AZURE_SCOPE
      value: api://prod-gcp.etterlatte.etterlatte-behandling/.default
  accessPolicy:
    outbound:
      rules:
        - application: etterlatte-behandling