no.nav.security.jwt {
  issuers = [
      {
         issuer_name = tokenx
         discoveryurl = ${TOKEN_X_WELL_KNOWN_URL}
         accepted_audience = ${TOKEN_X_CLIENT_ID}
      }
  ]
}
no.nav.security.jwt.client.registration {
  clients = [
    {
      client_name = tokenx
      well_known_url = ${TOKEN_X_WELL_KNOWN_URL}
      authentication = {
        client_id = ${TOKEN_X_CLIENT_ID}
        client_auth_method = private_key_jwt
        client_jwk = ${TOKEN_X_PRIVATE_JWK}
      }
    }
  ]
  cache = {
    enabled = true
    maximumSize = 1000
    evictSkew = 5
  }
}
no.nav.etterlatte.app.ventmedutgaaendekall = 30
no.nav.etterlatte.tjenester.pdl.url =  ${PDL_URL}
no.nav.etterlatte.tjenester.pdl.audience = ${PDL_AUDIENCE}
no.nav.etterlatte.tjenester.innsendtsoeknad.audience = ${INNSENDTSOEKNAD_AUDIENCE}
no.nav.etterlatte.tjenester.innsendtsoeknad.url = "http://innsendt-soeknad/api/"
no.nav.etterlatte.tjenester.kodeverk.url =  ${KODEVERK_URL}
no.nav.etterlatte.tjenester.kodeverk.navconsumerid =  ${NAV_CONSUMER_ID}
no.nav.etterlatte.tjenester.kodeverk.audience =  ${KODEVERK_AUDIENCE}
