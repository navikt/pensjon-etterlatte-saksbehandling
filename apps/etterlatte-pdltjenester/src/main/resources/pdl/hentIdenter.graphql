query(
    $ident: ID!,
    $grupper: [IdentGruppe!],
    $historikk: Boolean!
) {
    hentIdenter(ident: $ident, grupper: $grupper, historikk: $historikk) {
        identer {
            ident
            historisk
            gruppe
        }
    }
}

