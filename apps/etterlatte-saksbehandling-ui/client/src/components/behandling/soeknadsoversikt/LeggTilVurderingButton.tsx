import { Button } from '@navikt/ds-react'
import styled from 'styled-components'

export const LeggTilVurderingButton = styled(Button).attrs({ variant: 'secondary' })`
  height: fit-content;
  align-self: center;
`
