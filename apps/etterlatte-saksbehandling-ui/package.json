{
  "scripts": {
    "postinstall": "yarn --cwd server install && yarn --cwd client install",
    "prepare": "cd ../../ && husky install .husky"
  },
  "devDependencies": {
    "eslint": "8.53.0",
    "husky": "~8.0.1",
    "lint-staged": "~15.0.2",
    "prettier": "^3.0.3"
  }
}
